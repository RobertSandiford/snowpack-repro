
/*
import '~ReactRoutes'
import { Link } from './LinkFunction.js'


describe("Routing Module, Component Link", function ({it}) {
    
    it("Should produce an HTML <a> with inner text", function () {

        const linkText = "Home"

        //
        
        const { container } = render( <Link to="/">{linkText}</Link> )
        

        // check that it rendered an <a>
        screen.getByRole('link').should.not.be.null

        // check that the link text is what we passed in
        expect(
            container.textContent
        ).to.equal(linkText)
        
    })

})
*/
